app = "my-node-react-phonebook-app"
primary_region = "arn"

[env]
  MONGODB_URI = "mongodb+srv://riteshkumar1601:<password>@cluster0.4otkuyq.mongodb.net/phonebook?retryWrites=true&w=majority"
  PORT = "8080"
  NODE_ENV = "production"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[checks]
  [checks.alive]
    type = "tcp"
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"